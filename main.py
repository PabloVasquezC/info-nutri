import sqlite

# Llama a la función pasando el nombre de la tabla como una cadena
# sqlite.listar_tabla("verduras")

# LLama a la funcion insertar que recibe como parametros el nombre de la tabla el nombre, precio y temporada del alimento
# sqlite.insertar_registro()

# Llama a la funcion eliminar_registro_por_nombre que recibe por parametro el nombre de la tabla y el nombre del registro que deseas eliminar
# sqlite.eliminar_registro_por_nombre('verduras','berenjena')

# Llama a la funcion eliminar_registro_por_id que recibe por parametro el nombre de la tabla y el id del registro que deseas
# sqlite.eliminar_registro_por_id()

#Llama a la funcion listar_tabla_por_precio que recibe como parametro la tabla a ordenar por precio
sqlite.listar_tabla_por_precio('frutas')







