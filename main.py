import sqlite


# sqlite.ejecutar_query("select * from frutas")
# Muestra la tabla que se le pase por parametro (string)
sqlite.mostrar_tabla("frutas")

# LLama a la funcion insertar que recibe como parametros el nombre 
# de la tabla el nombre, precio y temporada del alimento
# sqlite.insertar_registro()

# Llama a la funcion eliminar_registro_por_nombre que recibe por parametro el 
# nombre de la tabla y el nombre del registro que deseas eliminar
# sqlite.eliminar_registro_por_nombre('verduras','berenjena')

# Llama a la funcion eliminar_registro_por_id que recibe por 
# parametro el nombre de la tabla y el id del registro que deseas
# sqlite.eliminar_registro_por_id()

#Llama a la funcion listar_tabla_por_precio que recibe como parametro la tabla a ordenar por precio
# sqlite.mostrar_tabla_por_precio('frutas')







